package com.renegade.weas.ui.home

import android.os.SystemClock
import com.renegade.weas.databinding.WeatherLayoutBinding

class WeatherLayoutHandler {
    private lateinit var binding :WeatherLayoutBinding
   fun updateValues(){
       dateInString = date.toString("yyyy/MM/dd HH:mm:ss")     val date = getCurrentDateTime()
       val

   }
}